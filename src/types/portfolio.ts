export interface TutorPortfolio {
  id: string;
  userId: string;
  headline: string;
  bio: string;
  university: string;
  department: string;
  experience: string;
  subjects: string;
  preferredClasses: string;
  expectedSalary: number;
  availability: string;
}
